/* Interview Room - glossary for hover tooltips.
   Key: the term as it appears in answers. Use "|" to list alternative spellings
   ("quantisation|quantization"); a plural "s"/"es" is matched automatically.
   Value: a 1-2 line plain-English definition. portal.js underlines the first
   mention of each term in a card's spoken answer and shows this on hover, focus
   or tap. Keep definitions short - they are a reminder, not a lesson. */

window.IR = window.IR || {};

window.IR.glossary = {
  /* ---------- LLM basics and inference ---------- */
  "token": "A small piece of text - a word or part of a word - that a model reads and writes. Models are priced and limited by tokens.",
  "context window": "The maximum amount of text (in tokens) a model can read in one request, including the prompt, documents and chat history.",
  "training cutoff|knowledge cutoff|cut-off date|cutoff date": "The date the model's training data ends. The model knows nothing that happened after it.",
  "prompt caching": "The provider stores the processed start of a prompt that repeats across requests, so later requests reuse it at lower cost and latency.",
  "next-token predictor|next-token prediction": "What an LLM really does: given the text so far, it predicts the most likely next word, again and again. It always produces something, even when it does not know.",
  "temperature": "A setting that controls randomness in the output. Low values give focused, repeatable answers; high values give more varied ones.",
  "top-p|nucleus sampling": "A sampling setting: the model picks only from the smallest set of next tokens whose probabilities add up to p.",
  "logit": "The raw score a model gives each possible next token, before it is turned into a probability.",
  "softmax": "A function that turns a list of raw scores into probabilities that add up to one.",
  "time to first token|TTFT": "How long the user waits before the first word of the answer appears. The main measure of how fast a chat feels.",
  "p95|p95 latency": "The response time that 95% of requests beat. It shows the slow experience that averages hide.",
  "throughput": "How many requests or tokens a system handles per second.",
  "prefill": "The first phase of generation, where the model reads the whole prompt in one pass before writing anything.",
  "KV cache": "Memory where the model keeps its processed view of earlier tokens, so it doesn't recompute them for every new token.",
  "speculative decoding": "A small, fast model drafts several tokens and the big model checks them in one step, which speeds up generation.",
  "continuous batching": "A serving technique that adds new requests to a running batch immediately instead of waiting for the batch to finish.",
  "PagedAttention": "vLLM's way of storing the KV cache in small pages, like an operating system manages memory, so GPU memory isn't wasted.",
  "vLLM": "An open-source engine for serving LLMs at high throughput on GPUs.",
  "quantisation|quantization": "Storing model weights or vectors with fewer bits (for example 4-bit instead of 16-bit) to save memory and speed things up, at a small accuracy cost.",
  "hallucination": "When a model states something false or unsupported as if it were true.",
  "structured output": "Forcing the model's reply to follow a fixed format, usually a JSON schema, so code can parse it reliably.",
  "JSON schema": "A description of the exact fields and types a JSON object must have. Used to validate model output.",
  "function calling|tool calling": "The model replies with a request to run a named function with arguments, which your code executes and returns.",
  "few-shot": "Including a few worked examples in the prompt so the model copies the pattern.",
  "chain-of-thought": "Asking the model to reason step by step before giving the final answer.",
  "context engineering": "Deciding exactly what information goes into the model's context for each step - and what stays out.",
  "system prompt": "The fixed instructions sent before the user's message that set the model's role, rules and format.",

  "System-1|System 1": "Fast, instinctive decision-making (from psychology), as opposed to System 2: slow, step-by-step reasoning. A System-1 model like Jev returns quick typed decisions; an LLM is System 2.",
  "RLCD": "Reinforcement Learning for Calibrated Decisions: TypeSafe AI's training method that rewards answers whose confidence matches how often they are right.",

  /* ---------- Transformers ---------- */
  "transformer": "The neural network architecture behind modern LLMs, built around attention.",
  "self-attention": "The mechanism that lets every token look at every other token in the input and decide which ones matter to it.",
  "multi-head attention": "Running several attention calculations in parallel, so each 'head' can focus on a different kind of relationship.",
  "positional encoding|RoPE": "Extra information added to tokens so the model knows their order in the sequence.",
  "encoder": "The part of a model that reads input and turns it into a representation. BERT is encoder-only.",
  "decoder": "The part of a model that generates text one token at a time. GPT-style LLMs are decoder-only.",

  /* ---------- Embeddings and retrieval ---------- */
  "embedding": "A list of numbers that captures the meaning of a piece of text, so similar meanings end up close together.",
  "embedding model": "A model that turns text into embeddings. Queries and documents must use the same one.",
  "vector database|vector store": "A database built to store embeddings and quickly find the ones closest to a query.",
  "cosine similarity": "A measure of how similar two embeddings are, based on the angle between them. 1 means same direction.",
  "semantic search": "Search by meaning using embeddings, so 'time off' can find 'leave policy' without shared words.",
  "BM25": "The classic keyword search ranking formula. Strong for exact terms like product codes and error IDs.",
  "hybrid search": "Running keyword (BM25) and vector search together and merging the results, to get both exact matches and meaning.",
  "reciprocal rank fusion|RRF": "A simple way to merge two ranked lists by position rather than by score, used in hybrid search.",
  "HNSW": "A popular index for fast approximate nearest-neighbour search. It builds a layered graph of vectors.",
  "IVF": "An index that groups vectors into clusters and only searches the nearest clusters, trading some recall for speed.",
  "approximate nearest neighbour|ANN": "Finding vectors that are very close to the query quickly, accepting that the exact closest might occasionally be missed.",
  "metadata filter": "Restricting a search by fields such as department, date, language or user permissions.",
  "chunk": "A small piece of a document, a few hundred tokens long, that is embedded and retrieved on its own. Splitting documents into chunks is RAG stage 2.",
  "chunking": "Splitting documents into chunks for retrieval. How you split decides what can be found.",
  "chunk overlap": "Repeating a little text between neighbouring chunks so a fact on a boundary isn't cut in half.",
  /* The seven RAG pipeline stages. The generic words are limited (only) to
     RAG, Advanced RAG and Embeddings cards, so "generate" in a Python card or
     a database "index" never shows a RAG definition. */
  "parse|parsing": { tip: "RAG stage 1: pulling clean text (and tables) out of files such as PDFs, Word or web pages. Scanned pages need OCR first.", only: ["rag", "ar", "em"] },
  "embed": "RAG stage 3: turning each chunk into an embedding - a list of numbers that captures its meaning - so similar texts end up close together.",
  "index|indexing": { tip: "RAG stage 4: storing the embeddings in a vector database with details like source, date and permissions, so they can be searched fast.", only: ["rag", "ar", "em"] },
  "top-k": "The number of best-matching results a search returns.",
  "reranker|reranking|rerank": "RAG stage 6: a second, more accurate model that re-orders the retrieved results so the most relevant ones come first.",
  "cross-encoder": "A model that reads the query and a document together and scores how relevant they are. Accurate, but slower.",
  "bi-encoder": "A model that embeds query and document separately, so documents can be embedded ahead of time. Fast, less precise.",
  "ColBERT": "A retrieval model that compares query and document word by word ('late interaction'). Between a bi-encoder and a cross-encoder.",
  "ColPali": "A retriever that searches page images directly instead of parsed text, useful for visual PDFs.",
  "Matryoshka": "Embeddings trained so you can cut them shorter and still keep most of their quality.",
  "query rewriting": "Using a model to rephrase the user's question into a better search query before retrieval.",
  "HyDE": "Hypothetical Document Embeddings: the model writes a fake answer first, and you search with that instead of the question.",
  "multi-hop": "A question that needs several lookups, where the second search depends on the first answer.",
  "GraphRAG": "RAG over a knowledge graph of entities and relationships, good for 'how are these connected' questions.",
  "knowledge graph": "Data stored as entities and the relationships between them, such as 'supplier - supplies - part'.",
  "agentic RAG": "RAG where an agent decides when and what to retrieve, possibly searching several times.",
  "lost in the middle": "Models tend to miss facts placed in the middle of a long context and focus on the start and end.",
  "contextual retrieval": "Adding a short summary of the whole document to each chunk before embedding it, so chunks keep their context.",
  "semantic cache": "A cache that reuses an earlier answer when a new question means the same thing, not only when the text is identical.",
  "OCR": "Optical character recognition: turning scanned images of text into machine-readable text.",
  "multimodal": "Handling more than one kind of input, such as text plus images, audio or tables.",
  "text-to-SQL": "Having a model turn a natural-language question into a SQL query against a database.",

  /* ---------- Evaluation ---------- */
  "golden set": "A fixed, versioned set of test questions with known correct answers, run on every change.",
  "LLM-as-a-judge|LLM judge": "Using one model to grade another model's answers against a rubric.",
  "rubric": "A marking guide that says what each score means, ideally with an example of each.",
  "faithfulness": "Whether every claim in the answer is supported by the retrieved context. The main hallucination check in RAG.",
  "groundedness": "How well an answer is backed by its source documents. Much the same idea as faithfulness.",
  "answer relevance": "Whether the answer actually addresses the question that was asked.",
  "context precision": "A RAG metric: how much of what was retrieved is actually useful, and whether the useful parts are ranked near the top.",
  "context recall": "A RAG metric: whether retrieval brought back everything the answer needs.",
  "recall@k": "How often the correct chunk appears anywhere in the top k results.",
  "precision@k": "What fraction of the top k results are relevant.",
  "MRR": "Mean reciprocal rank: rewards the first correct result for being near the top (rank 1 scores 1, rank 2 scores 0.5, and so on).",
  "nDCG": "A ranking metric that rewards relevant results for appearing higher, with graded relevance.",
  "RAGAS": "An open-source library of RAG evaluation metrics such as faithfulness and context recall, mostly scored by an LLM judge.",
  "Cohen's kappa": "A measure of agreement between two graders with lucky guesses removed. Used to check an LLM judge against humans.",
  "pass@k": "Whether a task succeeds at least once in k tries. Compare 'passes every time', which production needs.",
  "BLEU": "A score of word-sequence overlap with a reference answer, built for machine translation.",
  "ROUGE": "A score of how much of a reference text appears in the output, built for summarisation.",
  "BERTScore": "Compares output to a reference by meaning using embeddings, so paraphrases still score well.",
  "A/B test": "Showing two versions to different groups of real users and comparing a chosen metric.",
  "shadow run|shadow mode": "Sending real traffic to a new version in the background and comparing results, without showing users its answers.",
  "red teaming|red team": "Deliberately attacking your own system to find ways it can be made to misbehave before real attackers do.",
  "trajectory": "The full sequence of steps and tool calls an agent took to reach its result.",

  /* ---------- Operations and observability ---------- */
  "observability": "Recording enough detail - traces, logs, metrics, prompts and outputs - to explain why any single request behaved as it did.",
  "trace": "The full record of one request as it moves through every step of the system.",
  "span": "One step inside a trace, such as 'retrieve' or 'call the model', with its own inputs, outputs and timing.",
  "OpenTelemetry|OTel": "An open standard for collecting traces, metrics and logs, so you can send them to any tool without vendor lock-in.",
  "LangSmith": "LangChain's platform for tracing, evaluating and monitoring LLM apps.",
  "Langfuse": "An open-source, self-hostable platform for LLM tracing, prompt versioning and evaluation.",
  "canary": "Releasing a change to a small share of traffic first and watching it before rolling it out to everyone.",
  "rollback": "Returning quickly to the previous working version when a change causes problems.",
  "drift": "A gradual change over time - in user questions, data or model behaviour - that makes quality slip without any deploy.",
  "rate limit": "A cap the provider puts on how many requests or tokens you can send per minute.",
  "exponential backoff": "Waiting longer after each failed attempt (1s, 2s, 4s...) before retrying.",
  "jitter": "Random variation added to retry waits so many clients don't all retry at the same moment.",
  "circuit breaker": "A switch that stops calling a failing service for a while so failures don't pile up and spread.",
  "idempotency|idempotent": "Doing the same operation twice has the same effect as doing it once, so retries are safe.",
  "backpressure": "Slowing down or rejecting new work when a system is overloaded, instead of letting queues grow forever.",
  "model registry": "A catalogue of trained model versions, their metrics and which one is approved for production.",
  "MLflow": "An open-source tool for tracking experiments, storing models and managing a model registry.",
  "CI/CD": "Automated pipelines that test every change (CI) and deploy it safely (CD).",
  "data residency": "Rules that data must be stored and processed in a particular country or region.",
  "private endpoint": "A network connection to a cloud service that never crosses the public internet.",
  "Kubernetes": "A system that runs and manages containers across many machines - scheduling, restarts and scaling.",
  "Docker": "A tool that packages an application and its dependencies into a container image that runs the same everywhere.",
  "Terraform": "A tool for describing cloud infrastructure as code, so environments are reviewable and repeatable.",

  /* ---------- Agents, frameworks and protocols ---------- */
  "ReAct": "An agent pattern that alternates reasoning ('what should I do next?') with acting (calling a tool) and observing the result.",
  "MCP|Model Context Protocol": "An open protocol that lets AI applications connect to tools and data sources in a standard way.",
  "A2A": "Agent-to-Agent: a protocol for independent agents to discover each other and hand off tasks.",
  "LangChain": "A Python/JS framework with building blocks for LLM apps: models, prompts, retrievers and tools.",
  "LangGraph": "A framework for building agents as graphs of steps with shared state, loops, checkpoints and human approval.",
  "LCEL": "LangChain Expression Language: chaining components together with the pipe (|) operator.",
  "checkpointer": "In LangGraph, the component that saves the graph's state after each step so a run can pause, resume or recover.",
  "human-in-the-loop": "Pausing an automated flow so a person can review or approve a step before it continues.",
  "guardrails": "Checks around a model's input and output that block unsafe, off-topic or badly formatted content.",
  "prompt injection": "An attack where text in the input tries to override the system's instructions.",
  "indirect prompt injection|indirect injection": "Prompt injection hidden inside content the system reads, such as a retrieved document or web page.",
  "jailbreak": "A prompt designed to trick a model into ignoring its safety rules.",
  "PII": "Personally identifiable information: names, phone numbers, IDs and anything else that identifies a person.",

  /* ---------- Fine-tuning ---------- */
  "fine-tuning": "Further training a pre-trained model on your own examples to change its behaviour, style or format.",
  "LoRA": "Low-Rank Adaptation: fine-tuning a small set of added weights instead of the whole model, which is much cheaper.",
  "QLoRA": "LoRA on a model quantised to 4 bits, so large models can be fine-tuned on a single GPU.",
  "SFT|supervised fine-tuning": "Fine-tuning on example prompts paired with the ideal responses.",
  "DPO": "Direct Preference Optimisation: fine-tuning on pairs of a preferred and a rejected answer, without a separate reward model.",
  "RLHF": "Reinforcement learning from human feedback: training a model to produce answers people rate higher.",
  "distillation": "Training a smaller 'student' model to copy the outputs of a larger 'teacher' model.",
  "catastrophic forgetting": "When fine-tuning on a narrow task makes a model worse at things it used to do well.",

  /* ---------- ML fundamentals ---------- */
  "overfitting": "When a model learns the training data too closely, including its noise, and does worse on new data.",
  "bias-variance": "The trade-off between a model that is too simple (high bias) and one that is too sensitive to its training data (high variance).",
  "regularisation|regularization": "Adding a penalty during training that keeps the model simpler, to reduce overfitting.",
  "cross-validation": "Training and testing on several different splits of the data to get a more reliable estimate of performance.",
  "precision": "Of everything the model flagged as positive, the fraction that really was positive.",
  "recall": "Of all the real positives, the fraction the model managed to find.",
  "F1 score|F1": "A single score that balances precision and recall (their harmonic mean).",
  "ROC curve|ROC-AUC|ROC AUC": "Shows the true positive rate against the false positive rate across all thresholds. AUC summarises it in one number.",
  "PR-AUC": "The area under the precision-recall curve. More honest than ROC-AUC when positives are rare.",
  "calibration|calibrated": "Whether predicted probabilities match reality - of cases scored 0.8, about 80% should really be positive.",
  "SHAP": "A method that explains a single prediction by showing how much each feature pushed it up or down.",
  "gradient descent": "The training method that nudges model weights step by step in the direction that reduces error.",
  "learning rate": "How big each training step is. Too high and training is unstable; too low and it is slow.",
  "dropout": "Randomly switching off some neurons during training so the network doesn't rely on any single one.",
  "data leakage|feature leakage": "When information that won't be available at prediction time sneaks into training, making results look too good.",
  "class imbalance": "When one class is much rarer than the other, such as fraud at 0.1% of transactions.",
  "PCA": "Principal component analysis: compressing many features into a few that keep most of the variation.",
  "bagging": "Training many models on random samples of the data and averaging them. Reduces variance. Random Forest uses it.",
  "boosting": "Training models one after another, each fixing the previous one's mistakes. Reduces bias. XGBoost uses it.",

  /* ---------- Python ---------- */
  "GIL": "Python's Global Interpreter Lock: only one thread runs Python code at a time, so threads don't speed up CPU-heavy work.",
  "asyncio": "Python's library for running many I/O tasks concurrently on one thread using async and await.",
  "Pydantic": "A Python library that validates data against typed models. Often used to check LLM output.",
  "LRU cache": "A cache that throws away the least recently used item when it is full."
};
